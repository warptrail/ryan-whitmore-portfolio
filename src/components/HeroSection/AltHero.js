import React from 'react';
import { BackgroundOverlay, HeroBg, HeroContainer } from './HeroElements';

const AltHero = () => {
  return (
    <HeroContainer>
      <HeroBg>
        <BackgroundOverlay />
      </HeroBg>
    </HeroContainer>
  );
};

export default AltHero;
